# 零基础说明：这个补丁到底在做什么？怎么用？

你可以把它理解成：

- 游戏里有一个“光标/焦点”（你按方向键时，系统不知道该把选择框移动到哪里）。
- 某些界面（比如篝火、商店删牌）在你点了一张牌后，这个“焦点路线图”会断掉。
- 这个补丁的工作就是：**在你点牌后，帮游戏把焦点路线重新接上**。

---

## 一句话版原理
你点完牌后，补丁会做三件事：

1. 重新连接焦点邻居关系（让方向键知道“上/下该去哪”）
2. 让游戏刷新当前界面的导航上下文
3. 释放当前 GUI 焦点，让导航系统按新路线接管

所以效果就是：你再按方向键，焦点又能动了。

---

## 你不需要懂代码，只要会 3 步

## 第 1 步：准备文件
你最终要打包出一个 zip，里面通常有：

- `CampfireBugFix.dll`（C# 补丁逻辑）
- `CampfireBugFix.pck`（Godot 资源包）
- `CampfireBugFix.json`（mod 描述文件）
- `FocusPatchTargets.txt`（可选，写目标界面类名）

## 第 2 步：一键打包
在项目目录运行：

```powershell
pwsh ./tools/package_mod.ps1 -ModId CampfireBugFix -Version 1.1.0
```

成功后会得到：

- `dist/CampfireBugFix-1.1.0.zip`

## 第 3 步：放进 mods 目录测试
把 zip 解压，按你的 mod loader 规则放到 mods 文件夹。
进入游戏后测试：

- 篝火升级
- 删牌界面
- 变形界面
- 商店删牌

选中牌后按方向键，观察焦点是否能重新跳到牌组。

---

## 如果商店删牌还是没反应，怎么办？
不用慌，这通常是“版本类名不同”。

你做这几件事就行：

1. 打开日志，搜 `[CampfireNavigationParityPatch]`
2. 看有没有：
   - `未找到类型: NShop...`
   - `缺少方法: OnCardClicked / OnDeckCardClicked / OnCardSelected`
3. 把你真实日志贴出来，我就能帮你改 `FocusPatchTargets.txt`

---

## 你现在最该记住的一点
你不用会编程，也能推进：

- 你负责“运行 + 贴日志 + 说复现步骤”
- 我负责“根据日志改配置/改补丁”

这就是零基础协作的最快路径。
